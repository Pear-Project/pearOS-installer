node_modules
__pycache__/
*.pyc

# Dev-only Calamares source checkout used to build calamares-diskutility's
# binaries (see calamares-src/ for the 3.3.14 dev-loop copy, already
# committed) - 3.4.2 port for the forky target, ~400MB, not needed by the
# ISO build itself (only calamares-diskutility/modules-search-dir/'s
# already-built .so files are).
calamares-src-342/
